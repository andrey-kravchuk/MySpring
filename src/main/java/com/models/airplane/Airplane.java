package com.models.airplane;

import com.models.transport.Transport;

public class Airplane extends Transport {
    public Airplane(String factory, String model, int speed) {
        super(factory, model, speed);
    }
}
