package com.models.ship;

import com.models.transport.Transport;


public class Ship extends Transport {
    public Ship(String factory, String model, int speed) {
        super(factory, model, speed);
    }
}
